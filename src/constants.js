// Root endpoint for the public API
const PUBLIC_API_BASE_URL = 'https://api.endpass.com/v1';

// Root endpoint for the OAuth 2 API
const OAUTH2_IDENTITY_URL = 'https://identity.endpass.com';

module.exports = {
  PUBLIC_API_BASE_URL,
  OAUTH2_IDENTITY_URL,
};
